# Research Signals

This directory is reserved for reviewed public candidate-signal records that
conform to `schemas/research-signal.schema.json`. It intentionally contains no
raw captures. Start from `templates/research-signal.template.json`.

Contribution intake and automated monitoring are currently closed. Do not add
signals until the activation gates in `CONTRIBUTING.md` are approved.

